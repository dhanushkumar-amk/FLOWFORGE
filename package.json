{
  "name": "flowforge",
  "private": true,
  "packageManager": "npm@10.9.3",
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "docker:dev": "docker-compose up redis -d",
    "lint": "turbo lint"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "turbo": "^2.5.0"
  }
}
